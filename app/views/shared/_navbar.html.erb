<nav class="bg-white shadow-sm border-b border-gray-200" data-controller="navbar">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16 items-center">
      <div class="flex-shrink-0">
        <%= link_to t("app_name"), root_path, class: "text-xl font-bold text-gray-900" %>
      </div>

      <div class="hidden sm:flex sm:items-center sm:space-x-4">
        <% if user_signed_in? %>
          <span class="text-sm text-gray-600"><%= current_user.email %></span>
          <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-sm text-gray-600 hover:text-gray-900" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
        <% end %>
      </div>

      <button class="sm:hidden p-2 rounded-md text-gray-500 hover:text-gray-900 hover:bg-gray-100" data-action="navbar#toggle">
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>

    <div class="sm:hidden hidden pb-3" data-navbar-target="menu">
      <% if user_signed_in? %>
        <div class="py-2 text-sm text-gray-600"><%= current_user.email %></div>
        <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "block py-2 text-sm text-gray-600 hover:text-gray-900" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_path, class: "block py-2 text-sm text-gray-600 hover:text-gray-900" %>
      <% end %>
    </div>
  </div>
</nav>
